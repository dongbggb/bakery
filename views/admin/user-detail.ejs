<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Chi Tiết Người Dùng</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      font-family: 'Arial', sans-serif;
      background-color: #f0f2f5;
      color: #333;
    }
    .container {
      display: flex;
      min-height: 100vh;
    }
    .sidebar {
      width: 250px;
      background: #1a1a1a;
      color: white;
      padding: 2rem 0;
      box-shadow: 2px 0 10px rgba(0,0,0,0.1);
    }
    .sidebar-header {
      padding: 0 1.5rem 2rem;
      border-bottom: 1px solid rgba(255,255,255,0.2);
      font-size: 1.5rem;
      font-weight: bold;
      margin-bottom: 1rem;
    }
    .sidebar-menu {
      list-style: none;
    }
    .sidebar-menu li {
      margin: 0;
    }
    .sidebar-menu a {
      display: block;
      padding: 1rem 1.5rem;
      color: white;
      text-decoration: none;
      transition: background-color 0.3s;
      border-left: 3px solid transparent;
    }
    .sidebar-menu a:hover {
      background-color: rgba(255,255,255,0.1);
      border-left-color: white;
    }
    .sidebar-menu a.active {
      background-color: rgba(255,255,255,0.2);
      border-left-color: white;
    }
    .main-content {
      flex: 1;
      padding: 2rem;
      overflow-y: auto;
    }
    h1 {
      margin-bottom: 2rem;
      font-size: 1.8rem;
    }
    .card {
      background: white;
      padding: 2rem;
      border-radius: 8px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
      margin-bottom: 2rem;
    }
    .user-info {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 2rem;
      margin-bottom: 2rem;
    }
    .info-group {
      padding-bottom: 1rem;
    }
    .info-label {
      font-weight: bold;
      color: #666;
      font-size: 0.9rem;
    }
    .info-value {
      font-size: 1.1rem;
      color: #333;
      margin-top: 0.3rem;
    }
    .role-badge {
      padding: 0.4rem 0.8rem;
      border-radius: 4px;
      font-size: 0.85rem;
      text-align: center;
      font-weight: bold;
      display: inline-block;
    }
    .role-admin {
      background-color: #333;
      color: white;
    }
    .role-user {
      background-color: #999;
      color: white;
    }
    .role-form {
      display: flex;
      gap: 1rem;
      align-items: center;
      margin-bottom: 2rem;
      padding-top: 2rem;
      border-top: 2px solid #ddd;
    }
    .role-form select {
      padding: 0.5rem;
      border: 1px solid #ddd;
      border-radius: 4px;
    }
    table {
      width: 100%;
      border-collapse: collapse;
    }
    th, td {
      padding: 1rem;
      text-align: left;
      border-bottom: 1px solid #ddd;
    }
    th {
      background-color: #f5f5f5;
      font-weight: bold;
    }
    .btn {
      padding: 0.5rem 1rem;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      text-decoration: none;
      display: inline-block;
      background-color: #000;
      color: white;
      font-size: 0.9rem;
    }
    .btn:hover {
      background-color: #333;
    }
    .btn-danger {
      background-color: #555;
    }
    .btn-danger:hover {
      background-color: #333;
    }
    .btn-back {
      background-color: #6c757d;
    }
    .btn-back:hover {
      background-color: #5a6268;
    }
    .empty-message {
      text-align: center;
      padding: 2rem;
      color: #666;
    }
  </style>
</head>
<body>
  <div class="container">
    <aside class="sidebar">
      <div class="sidebar-header">Admin Panel</div>
      <ul class="sidebar-menu">
        <li><a href="/admin/dashboard">Dashboard</a></li>
        <li><a href="/admin">Sản Phẩm</a></li>
        <li><a href="/admin/orders">Đơn Hàng</a></li>
        <li><a href="/admin/users" class="active">Người Dùng</a></li>
        <li><a href="/admin/discounts">Mã Giảm Giá</a></li>
        <li><a href="/">Trang Chủ</a></li>
        <li><a href="/auth/logout">Đăng Xuất</a></li>
      </ul>
    </aside>

    <main class="main-content">
      <h1>Chi Tiết Người Dùng</h1>

      <div class="card">
        <div class="user-info">
          <div>
            <div class="info-group">
              <div class="info-label">Tên</div>
              <div class="info-value"><%= targetUser.name %></div>
            </div>
            <div class="info-group">
              <div class="info-label">Email</div>
              <div class="info-value"><%= targetUser.email %></div>
            </div>
          </div>
          <div>
            <div class="info-group">
              <div class="info-label">Điện Thoại</div>
              <div class="info-value"><%= targetUser.phone || 'Không có' %></div>
            </div>
            <div class="info-group">
              <div class="info-label">Địa Chỉ</div>
              <div class="info-value"><%= targetUser.address || 'Không có' %></div>
            </div>
          </div>
        </div>

        <div class="role-form">
          <label style="font-weight: bold;">Role hiện tại:</label>
          <span class="role-badge role-<%= targetUser.role %>">
            <%= targetUser.role === 'admin' ? 'Admin' : 'User' %>
          </span>
          <form action="/admin/user/<%= targetUser._id %>/role" method="POST" style="display: flex; gap: 1rem; align-items: center; margin-left: auto;">
            <select name="role">
              <option value="user" <%= targetUser.role === 'user' ? 'selected' : '' %>>User</option>
              <option value="admin" <%= targetUser.role === 'admin' ? 'selected' : '' %>>Admin</option>
            </select>
            <button type="submit" class="btn">Cập Nhật Role</button>
          </form>
        </div>

        <h2 style="margin-top: 2rem; margin-bottom: 1rem;">📦 Lịch Sử Đơn Hàng</h2>
        <% if (userOrders.length === 0) { %>
          <div class="empty-message">
            <p>Người dùng này chưa có đơn hàng nào</p>
          </div>
        <% } else { %>
          <table>
            <thead>
              <tr>
                <th>Mã Đơn</th>
                <th>Ngày Đặt</th>
                <th>Số Lượng</th>
                <th>Tổng Tiền</th>
                <th>Trạng Thái</th>
                <th>Hành Động</th>
              </tr>
            </thead>
            <tbody>
              <% userOrders.forEach(order => { %>
                <tr>
                  <td>#<%= order._id.toString().slice(-8) %></td>
                  <td><%= new Date(order.createdAt).toLocaleDateString('vi-VN') %></td>
                  <td><%= order.items.length %> sản phẩm</td>
                  <td><%= order.totalPrice.toLocaleString('vi-VN') %>đ</td>
                  <td><%= order.status %></td>
                  <td>
                    <a href="/admin/order/<%= order._id %>" class="btn">Chi Tiết</a>
                  </td>
                </tr>
              <% }); %>
            </tbody>
          </table>
        <% } %>

        <div style="margin-top: 2rem; display: flex; gap: 1rem;">
          <a href="/admin/users" class="btn btn-back">← Quay Lại</a>
          <form action="/admin/user/<%= targetUser._id %>/delete" method="POST" style="display: inline;" onsubmit="return confirm('Bạn chắc chắn muốn xóa người dùng này?');">
            <button type="submit" class="btn btn-danger">🗑️ Xóa Người Dùng</button>
          </form>
        </div>
      </div>
    </main>
  </div>
</body>
</html>
